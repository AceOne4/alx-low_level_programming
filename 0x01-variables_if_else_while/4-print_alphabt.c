#include <stdio.h>
/**
 * main -  that is alphapeat game that shows all alphbaet
 * main: prints alphabet
 * Return: 0 (Succes)
 */
int main(void)
{
	char alp;

	for (alp = "a"; alp <= "z"; alp++)
	{
		if (alp != "q" && alp != "e")
		{
			putchar(alp[i]);
		}
	}
	putchar('\n');
	return (0);
}
