#include "main.h"
/**
 * print_alphabet - print all alphabeat in lower case
 * print_alphabet: is function to print all alphabeat in lower case
 */

void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; a++)
	{
		_putchar(letter);
	}
	_putchar('\n');
}
