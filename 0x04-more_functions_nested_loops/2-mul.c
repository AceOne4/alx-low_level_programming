#include "main.h"

/**
 * mul - is function the multply any two numbers and show the result
 * @a: is first num
 * @b: is the sec num
 * Return: the result of multpll
 */
int mul(int a, int b)
{
	return (a * b);
}
