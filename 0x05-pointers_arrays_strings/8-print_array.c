#include "main.h"
#include <stdio.h>

/**
 * print_array - print number of array elment
 * @a: the array
 * @n: number of elemnt of the array
 */
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf("%d", a[j]);
		if (i != (n - 1))
		{
			printf(", ")
		}
	}
	printf("\n")
}
